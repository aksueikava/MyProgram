<Page x:Class="MyProgram.MPVM.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MyProgram.MPVM.Pages"
      xmlns:viewmodels="clr-namespace:MyProgram.MPVM.ViewModels.Pages" 
      d:DataContext="{d:DesignInstance Type=viewmodels:MainPageViewModel}"
      mc:Ignorable="d">

    <Page.DataContext>
        <viewmodels:MainPageViewModel/>
    </Page.DataContext>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{Binding PageTitle}" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0"/>
            <Button Content="Перейти на Test Page" Command="{Binding NavigateToTestPageCommand}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>